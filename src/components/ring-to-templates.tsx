import React from 'react';

export const RingToTemplates = props => {
    // hit the back end for the ring to templates
    const renderTemplates = (templates: string) => { return "" };
    let content = (
        <div id="RingToTemplates">

        </div>
    );
    return content;
}